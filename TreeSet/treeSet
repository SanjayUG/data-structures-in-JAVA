
// TreeSet is like a magical tree that grows in a perfectly ordered manner in Java. It's part of Java's Collections Framework and implements the Set interface, meaning it only holds unique elements.

// -------------------------------------------------

// Creating a TreeSet 
TreeSet<Integer> treeSet = new TreeSet<>();

// Adding elements
treeSet.add(5);
treeSet.add(2);
treeSet.add(8); 
treeSet.add(1);

// Removing an element 
treeSet.remove(2);

// Checking if an element exists 
boolean containsFive = treeSet.contains(5);

// Getting the first and last elements 
int first = treeSet.first(); 
int last = treeSet.last();
