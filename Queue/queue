
// Queues in Java are essentially a collection that follows the First-In-FirstOut (FIFO) principle, much like a line at a ticket counter. The element that's
added first is the one that gets removed first.

// -----------------------------------------------

// Initialization:
Queue<String> queue = new LinkedList<>();

// Adding Elements:
queue.add("Element1");
queue.offer("Element2");

// Removing Elements:
String removedElement = queue.remove();
String polledElement = queue.poll();

// Peeking at the Head:
String headElement = queue.element();
String headPeek = queue.peek();

// Size of the Queue:
int size = queue.size();

// Checking if Queue Contains an Element:
boolean exists = queue.contains("Element1");

// Removing All Elements:
queue.clear();
