
// A Deque (Double Ended Queue) is a special type of queue that allows insertion and removal of elements from both ends. The Deque interface in Java extends the Queue interface and is part of the Java Collections Framework.

// ------------------------------------------------------

// Creating a Deque:

Deque<Integer> arrayDeque = new ArrayDeque<>(); 
// OR
Deque<Integer> linkedListDeque = new LinkedList<>();

// Adding to front
deque.addFirst(1);
// adds and return boolean
boolean success = deque.offerFirst(16);
deque.offerFirst(12);

// Add to the end:
deque.addLast(8);
deque.offerLast(10);

// Remove from the front:
deque.pollFirst();

// Remove from the end:
deque.pollLast();

// Peek at the first element:
Integer first = deque.peekFirst();

// Peek at the last element:
Integer last = deque.peekLast();

