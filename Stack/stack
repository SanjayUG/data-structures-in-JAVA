
// A Stack in Java is a collection that follows the Last-In-First-Out (LIFO)
principle. Think of it like a stack of books: you can only take the top book off
and add a new one on top.

// --------------------------------------------------

// Initialization:
Stack<String> stack = new Stack<>();

// Pushing Elements:
stack.push("Element1");
stack.push("Element2");

// Popping Elements:
String topElement = stack.pop();

// Peeking at the Top Element:
String topElement = stack.peek();

// Checking if Stack is Empty:
boolean isEmpty = stack.isEmpty();

// Searching for an Element: return position (int)
int position = stack.search("Element1");

// Check if "Banana" exists, returns tru or false
boolean exists = stack.contains("Banana");

// Iteration:
for (String item : stack) {
 System.out.println(item);
}
